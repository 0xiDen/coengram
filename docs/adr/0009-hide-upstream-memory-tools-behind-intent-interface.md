# Hide upstream memory tools behind an intent interface

Claude and other remote callers receive authorization-aware recall, retain, propose, review, correct, and erasure-request operations rather than the upstream entity, fact, graph-export, or raw-Cypher surface. Neo4j Agent Memory remains an internal adapter so tenant, Actor, Subject User, Promotion, provenance, and audit rules are enforced once and storage mechanics cannot bypass them.
