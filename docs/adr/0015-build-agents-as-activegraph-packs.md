# Build agents as ActiveGraph Packs

A deep Agent Runtime module constructs ActiveGraph runtimes with tenant persistence, Frame, Budget, model provider, recorded tools, metrics, and replay policy. Agent capabilities are native versioned Packs of typed objects, behaviors, tools, prompts, policies, and settings; the first is `knowledge_synthesis`, and future agents reuse the runtime module instead of inheriting from a shallow base class.
